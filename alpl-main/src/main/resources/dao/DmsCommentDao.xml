<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ncepu.alpl.dao.DmsCommentDao">

    <resultMap id="CommentDetailMap"
               type="cn.ncepu.alpl.domain.DmsCommentDetail"
               extends="cn.ncepu.alpl.mapper.DmsCommentMapper.BaseResultMap">
        <association property="fromUser"
                     resultMap="cn.ncepu.alpl.mapper.UmsUserMapper.BaseResultMap"
                     columnPrefix="u_">

        </association>
    </resultMap>

    <update id="updateIncrCommentReplyCount">
        UPDATE dms_comment
            SET comment_reply_count = comment_reply_count + #{count}
        WHERE id = #{commentId}
    </update>

    <delete id="deleteByIdAndFromUserId">
        DELETE FROM dms_comment
        WHERE id = #{commentId} AND from_user_id = #{userId}
    </delete>

    <select id="selectDetailBySectionId"
            resultMap="CommentDetailMap">
        SELECT c.*,
               u.id u_id,
               u.username u_username,
               u.nickname u_nickname,
               u.avatar u_avatar
        FROM dms_comment c
                 LEFT JOIN ums_user u
                           ON c.from_user_id = u.id
        WHERE section_id = #{sectionId}
        ORDER BY c.topping_sort DESC,
        <choose>
            <when test="isOrderByLikes">
                c.stars DESC
            </when>
            <when test="isOrderByCreateTime">
                c.create_time DESC
            </when>
            <otherwise>
                c.create_time DESC
            </otherwise>
        </choose>
    </select>

    <select id="selectBySectionIdForUpdate"
            resultMap="cn.ncepu.alpl.mapper.DmsCommentMapper.BaseResultMap">
        SELECT id
        FROM dms_comment
        WHERE section_id = #{sectionId}
            FOR UPDATE
    </select>

    <select id="selectMaxToppingSortBySectionId" resultType="java.lang.Integer">
        SELECT IFNULL(MAX(topping_sort), 0)
        FROM dms_comment
        WHERE section_id = #{sectionId}
    </select>

    <update id="updateIncrStars">
        UPDATE dms_comment
        SET stars = stars + #{count}
        WHERE id = #{id}
    </update>

</mapper>
